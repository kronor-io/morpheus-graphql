module Data.Morpheus.Client.Transform
  ( toGlobalDefinitions,
    toGlobalDefinitionsWith,
    toLocalDefinitions,
  )
where

import Data.Morpheus.Client.Transform.Global (toGlobalDefinitions, toGlobalDefinitionsWith)
import Data.Morpheus.Client.Transform.Local
  ( toLocalDefinitions,
  )
